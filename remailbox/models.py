from django.db import models
from user.models import User

# Create your models here.

# class List_user(models.Model):
#     user = models.ForeignKey(User, on_delete=models.CASCADE)

#     def __str__(self):
#         return str(self.user.subject)